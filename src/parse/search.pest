search = _{ soi ~ expression ~ eoi }

expression = { (
  quote |
  group |
  id |
  word
)+ }

quote = @{ "\"" ~ (!"\"" ~ any)* ~ "\"" }
group = { "(" ~ expression? ~ ")" }
id = @{ "#" ~ ('0'..'9')+ }
word = @{ (!(" " | "(" | ")") ~ any)+ }

whitespace = _{ " " }
